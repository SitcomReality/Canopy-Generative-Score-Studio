// Entry point for layer editing actions. The implementation lives in
// ./layers/, split by concern (grid edits, composition, lifecycle, sound,
// reactive schema).
export { createLayerActions } from "./layers/index.js";
