// Entry point for the exported score emitters: the DATA-ONLY score module plus
// the SHARED engine source. The implementation lives in ./runtime-module/.
export { runtimeModule, scoreEngineSource } from "./runtime-module/index.js";
